const server = "http://localhost:3020/"

let serverLinks = {
    nasaToday : `${server}nasaToday`,
    nasaLibrary : `${server}nasaLibrary/`,
    nasaMedia: `${server}nasaMedia`,
    savedPosts: `${server}savedPosts`
}

export default serverLinks